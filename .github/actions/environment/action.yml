name: Environment
description: Set up the environment

runs:
  using: "composite"
  steps:
    - name: Install UV
      uses: astral-sh/setup-uv@v8.2.0

    - name: Install Dependencies
      shell: bash
      run: uv sync --frozen
